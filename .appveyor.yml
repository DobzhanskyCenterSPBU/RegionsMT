image: ubuntu

install:
    - cd /home/appveyor/projects
    - git clone https://github.com/ampl/gsl
    - bash regionsmt/.install/gsl.sh
    - cd regionsmt

test_script:
    - make
    - ./RegionsMT-Release -T
    - ./RegionsMT-Debug -T
    - make clean


