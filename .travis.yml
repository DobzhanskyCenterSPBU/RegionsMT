os: linux
dist: xenial
language: c

before_install:
  - md RegionsMT
  - cd RegionsMT
#  - sudo apt-get update -qq
#  - sudo apt-get install make

compiler: gcc
#  - clang
#  - gcc

#os:
#  - linux
#  - osx

install:

script: make # ./configure && make && make test
